/*const highlightCurrentLink = () => {
    for (let i = 1; i <= $('ul.highlightCurrentLink li').length; i++) {
        let listItem = $(`ul.highlightCurrentLink li:nth-child(${i})`);
        if (listItem === location.href) {
            listItem.css('color', 'var(--current-url-color)');
        }
    }
}  --JQUERY  

Its supposed to highlight the current url's link.   I think it is because the remote link doesn't work, but I don't want to download anything to the folder.

Doesn't work*/
console.log('Hi');